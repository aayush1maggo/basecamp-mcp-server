# Basecamp OAuth Application Credentials
# Create your OAuth app at: https://launchpad.37signals.com/integrations
BASECAMP_CLIENT_ID=your_client_id_here
BASECAMP_CLIENT_SECRET=your_client_secret_here
BASECAMP_REDIRECT_URI=http://localhost:8000/auth/callback

# Basecamp Account Information
# Find this in your Basecamp 3 URL: https://3.basecamp.com/ACCOUNT_ID/projects
BASECAMP_ACCOUNT_ID=your_account_id_here

# User Agent (required by Basecamp API)
# Format: "App Name (your@email.com)"
USER_AGENT="Basecamp MCP Server (your@email.com)"

# === TOKEN CONFIGURATION ===
# The server supports two methods for providing access tokens:
#
# METHOD 1: Environment Variables (recommended for FastMCP Cloud)
# Set these environment variables with your OAuth tokens:
BASECAMP_ACCESS_TOKEN=your_access_token_here
BASECAMP_REFRESH_TOKEN=your_refresh_token_here
BASECAMP_TOKEN_EXPIRES_AT=2025-12-31T23:59:59Z
#
# METHOD 2: token.json file (recommended for local development)
# Create a token.json file with your tokens (server will auto-detect and use it)
#
# Priority: Environment variables are checked first, then token.json file

# Application Secrets (optional - for OAuth flow app, not needed for MCP server)
FLASK_SECRET_KEY=your_flask_secret_here
MCP_API_KEY=your_mcp_api_key_here 